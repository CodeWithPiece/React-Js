import React from "react";

const NotFound = () => {
  return (
    <div className="container">
      <h1 className="mt-5">Sticky footer</h1>
      <p className="lead">
        Pin a footer to the bottom of the viewport in desktop browsers with this
        custom HTML and CSS.
        <br />
        Use{" "}
        <a href="/docs/5.3/examples/sticky-footer-navbar/">
          the sticky footer with a fixed navbar
        </a>{" "}
        if need be, too.
      </p>
    </div>
  );
};

export default NotFound;
